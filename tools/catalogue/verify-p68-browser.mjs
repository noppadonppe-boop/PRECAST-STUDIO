import {chromium} from '@playwright/test';import {createCatalogueServer} from './server.mjs';import {resolve} from 'node:path';import {mkdir,writeFile} from 'node:fs/promises';import assert from 'node:assert/strict';
const root=resolve(import.meta.dirname,'../..'),origin='http://127.0.0.1:5199',out=resolve(root,'output/stage5-library-p68/qa');await mkdir(out,{recursive:true});const {server,entryToken}=await createCatalogueServer({root,origin,ownerPreview:true});await new Promise(r=>server.listen(5199,'127.0.0.1',r));const browser=await chromium.launch({headless:true,channel:'chrome'});
try{const ctx=await browser.newContext({viewport:{width:1440,height:1000},acceptDownloads:true}),page=await ctx.newPage(),errors=[];page.on('pageerror',e=>errors.push(e.message));assert.equal((await ctx.request.get(origin+'/api/catalogue/moulds')).status(),401);await page.goto(origin+'/catalogue#access='+entryToken);await page.getByRole('button',{name:'แบบและข้อมูลชุดใหม่',exact:true}).click();await page.getByRole('button',{name:'แม่แบบ',exact:true}).click();await page.getByText('พบ 44 setup',{exact:true}).waitFor();const data=await(await ctx.request.get(origin+'/api/catalogue/moulds')).json();assert.equal(data.supplementRevision,'P68');
for(const family of ['A','B','D']){const back=page.getByRole('button',{name:'← กลับชุดแม่แบบ',exact:true});if(await back.count())await back.click();const id=`MF-${family}-H15-RH-W01-P08-P38`;await page.getByLabel('ค้นหาแม่แบบ').fill(id);await page.locator('.cat-card .cat-board').click();const s=data.setups.find(s=>s.id===id);await page.getByRole('heading',{name:'P68 — รายละเอียดเพิ่มเติม P62–P67',exact:true}).scrollIntoViewIfNeeded();for(const rev of ['P66','P67']){const g=s.supplementGroups.find(g=>g.revision===rev),img=page.getByRole('img',{name:s.typicalId+' '+rev+' '+g.label,exact:true});await img.scrollIntoViewIfNeeded();await img.evaluate(async e=>{await e.decode();if(!e.naturalWidth)throw Error('empty image');});}const g=s.supplementGroups.find(g=>g.revision==='P67'),csv=g.files.find(f=>f.filename.endsWith('.csv')),dp=page.waitForEvent('download');await page.getByRole('button',{name:'P67 · '+csv.filename,exact:true}).click();assert.equal(await(await dp).failure(),null);await page.screenshot({path:resolve(out,family+'-desktop.png')});}
await page.setViewportSize({width:390,height:844});assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.screenshot({path:resolve(out,'mobile.png')});assert.deepEqual(errors,[]);await writeFile(resolve(out,'audit.json'),JSON.stringify({families:['A','B','D'],loadedLatestImages:6,csvDownloads:3,anonymousStatus:401,mobileOverflow:false,pageErrors:errors},null,2));console.log('P68 browser checks passed');}finally{await browser.close();await new Promise(r=>server.close(r));}
