import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {root} from './cap-geometry-p55.mjs';
const out=path.join(root,'output/stage5-update-p67');fs.mkdirSync(out,{recursive:true});fs.cpSync(path.join(root,'output/abd-wall-gusset-p67'),path.join(out,'abd-wall-gusset-p67'),{recursive:true});fs.mkdirSync(path.join(out,'source-scripts'),{recursive:true});
for(const n of ['abd-wall-gusset-p67.mjs','abd-gusset-boards-p67.mjs','abd-wall-gusset-p67.test.mjs'])fs.copyFileSync(path.join(root,'tools/modular-program',n),path.join(out,'source-scripts',n));
fs.copyFileSync(path.join(root,'knowledge/modular-program-r02/STAGE5_CONTINUATION_P67.md'),path.join(out,'KNOWLEDGE.md'));
fs.writeFileSync(path.join(out,'test-results.txt'),execFileSync(process.execPath,['--test','tools/modular-program/abd-wall-gusset-p67.test.mjs'],{cwd:root,encoding:'utf8'}));
fs.writeFileSync(path.join(out,'README.md'),'# P67 — wall foot gussets\n\nOpen abd-wall-gusset-p67/index.html. Twelve coordinated model-derived boards, JSON geometry and gusset-stock CSVs. Braces, seam locks and structural/handling/fabrication checks remain incomplete. Closed waler windows require a coordinated fabrication sequence and tolerances. No fabrication/casting/lifting release. Scripts require the existing project. Website remains P61 pending integrated hardware.\n');
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]),files=walk(out).filter(p=>path.relative(out,p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(path.join(out,'manifest.json'),JSON.stringify({revision:'P67',stage:5,stageComplete:false,files,engineeringApproved:false,productionReleased:false},null,2));console.log({files:files.length});
