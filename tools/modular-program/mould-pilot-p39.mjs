import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {createRequire} from 'node:module';import {fileURLToPath} from 'node:url';
import {raster} from './drawing-raster-p36.mjs';import {volumeCg,hash,root} from './stage5-p38.mjs';
export const out=path.join(root,'output/mould-pilot-p39');
const source='output/stage5-moulds-p38/MF-C-H15-LH-W01-P05-P38/setup.json';
const s=JSON.parse(fs.readFileSync(path.join(root,source)));const H=1485,T=6;
const box=(x0,x1,y0,y1,z0,z1)=>({poly:[[x0,y0],[x1,y0],[x1,y1],[x0,y1]],z0,z1});
const arc=(ri,ro,z0,z1)=>Array.from({length:32},(_,i)=>{const p=(r,j)=>[400-r*Math.cos(j*Math.PI/64),2425+r*Math.sin(j*Math.PI/64)];return {poly:[p(ro,i),p(ri,i),p(ri,i+1),p(ro,i+1)],z0,z1};});
export const cellFaces=c=>{const a=c.poly.map(p=>[...p,c.z0]),b=c.poly.map(p=>[...p,c.z1]);return [[...a].reverse(),b,...a.map((v,i)=>[v,a[(i+1)%a.length],b[(i+1)%a.length],b[i]])];};
const add=(a,b)=>a.map((v,i)=>v+b[i]);
const shifted=(c,v)=>({poly:c.poly.map(p=>[p[0]+v[0],p[1]+v[1]]),z0:c.z0+v[2],z1:c.z1+v[2]});
const colors=['#344e68','#2c6594','#3983b3','#4a93be','#538aa2','#669db2','#82b4c4','#a25462','#bb7280','#d79531','#e6ab4b','#eabd66','#ce8229'];
function unit(id,name,detail,cells,exploded){return {id,name,detail,cells,exploded,color:colors[parts.length],materialProposal:'STEEL_GRADE_PENDING',engineeringApproved:false};}
const parts=[];const put=(...a)=>parts.push(unit(...a));
const ribs=[150,738.5,1327];
put('M00','ฐานหล่อ 1 ชุด','แผ่นฐาน 1900×3225×12; ซี่รองลึก150 หนา8 (เสนอ)',[box(-200,1700,-200,3025,-12,0),...[-120,1570].map(x=>box(x,x+8,-200,3025,-162,-12)),...[-150,800,1800,2950].map(y=>box(-200,1700,y,y+8,-162,-12))],[0,0,-400]);
put('M01','แบบผนังนอก','หน้าสัมผัส x=0; 2425×1485; skin6 + rib80×8',[box(-T,0,0,2425,0,H),...ribs.map(z=>box(-86,-6,0,2425,z,z+8))],[-750,0,0]);
put('M02','แบบมุมโค้งนอก','รัศมีสัมผัส400; skin6 + rib80×8',[...arc(400,406,0,H),...ribs.flatMap(z=>arc(406,486,z,z+8))],[-700,700,0]);
put('M03','แบบหลังคานอก','หน้าสัมผัส y=2825; 1090×1485; skin6',[box(400,1490,2825,2831,0,H),...ribs.map(z=>box(400,1490,2831,2911,z,z+8))],[0,750,0]);
put('M04','แบบผนังใน','หน้าสัมผัส x=150; 2425×1485; skin6',[box(150,156,0,2425,0,H),...ribs.map(z=>box(156,236,0,2425,z,z+8))],[650,0,0]);
put('M05','แบบมุมโค้งใน','รัศมีสัมผัส250; skin6 + rib80×8',[...arc(244,250,0,H),...ribs.flatMap(z=>arc(164,244,z,z+8))],[650,-650,0]);
put('M06','แบบหลังคาใน','หน้าสัมผัส y=2675; 1090×1485; skin6',[box(400,1490,2669,2675,0,H),...ribs.map(z=>box(400,1490,2589,2669,z,z+8))],[0,-650,0]);
put('M07','แบบปิดปลายผนัง','ผิวปิดคอนกรีต 150×1485; skin6',[box(-6,156,-6,0,0,H)],[0,-550,0]);
put('M08','แบบปิดปลายหลังคา','ผิวปิดคอนกรีต 150×1485; skin6',[box(1490,1496,2669,2831,0,H)],[650,0,0]);
put('W01','บล็อกหน้าต่างล่าง','150×1200×6 (พิกัดท่าหล่อ)',[box(0,150,900,2100,292.5,298.5)],[0,0,-150]);
put('W02','บล็อกหน้าต่างบน','150×1200×6 (พิกัดท่าหล่อ)',[box(0,150,900,2100,1186.5,1192.5)],[0,0,350]);
put('W03','บล็อกหน้าต่างด้าน y-','150×6×888; ประกบระหว่าง W01/W02',[box(0,150,900,906,298.5,1186.5)],[-350,-120,0]);
put('W04','บล็อกหน้าต่างด้าน y+','150×6×888; ประกบระหว่าง W01/W02',[box(0,150,2094,2100,298.5,1186.5)],[-350,120,0]);
// Collision cells reproduce the source tessellated concrete, not its bounding box.
const concrete=[box(0,150,0,900,0,H),box(0,150,900,2100,0,292.5),box(0,150,900,2100,1192.5,H),box(0,150,2100,2425,0,H),...arc(250,400,0,H),box(400,1490,2675,2825,0,H)];
const axes=c=>c.poly.map((p,i)=>{const q=c.poly[(i+1)%c.poly.length],d=[q[0]-p[0],q[1]-p[1]],l=Math.hypot(...d);return [-d[1]/l,d[0]/l,0];});
const interval=(c,n)=>{const vals=c.poly.flatMap(p=>[p[0]*n[0]+p[1]*n[1]+c.z0*n[2],p[0]*n[0]+p[1]*n[1]+c.z1*n[2]]);return [Math.min(...vals),Math.max(...vals)];};
// Continuous SAT for translational convex vertical prisms; strict penetration only.
// No time-step sampling. Boundary contact within 0.001mm is not interference.
export function swept(a,b,move){let lo=0,hi=1;const eps=.001;for(const n of [...axes(a),...axes(b),[0,0,1]]){const [a0,a1]=interval(a,n),[b0,b1]=interval(b,n),v=n.reduce((q,k,i)=>q+k*move[i],0),L=b0+eps-a1,U=b1-eps-a0;if(Math.abs(v)<1e-12){if(!(L<0&&U>0))return false;}else{const t0=L/v,t1=U/v;lo=Math.max(lo,Math.min(t0,t1));hi=Math.min(hi,Math.max(t0,t1));if(lo>=hi)return false;}}return lo<hi;}
const collide=(aa,bb,d=[0,0,0])=>aa.some(a=>bb.some(b=>swept(a,b,d)));
const moves=[['M07',[0,-300,0]],['M08',[300,0,0]],['M01',[-500,0,0]],['M03',[0,500,0]],['M02',[-500,500,0]],['M04',[400,0,0]],['M06',[0,-400,0]],['M05',[400,-400,0]],['W03',[0,20,0],[-250,0,0]],['W04',[0,-20,0],[-250,0,0]],['W01',[0,0,20],[-250,0,0]],['W02',[0,0,-20],[-250,0,0]]];
export function check(){const initial=[];for(let i=0;i<parts.length;i++){if(collide(parts[i].cells,concrete))initial.push(parts[i].id+' vs concrete');for(let j=i+1;j<parts.length;j++)if(collide(parts[i].cells,parts[j].cells))initial.push(parts[i].id+' vs '+parts[j].id);}
const active=new Map(parts.map(p=>[p.id,p.cells])),steps=[];for(const [id,...legs]of moves){let cells=active.get(id);for(const delta of legs){const hits=[];if(collide(cells,concrete,delta))hits.push('CONCRETE');for(const [other,solid]of active)if(other!==id&&collide(cells,solid,delta))hits.push(other);steps.push({part:id,deltaMm:delta,hits});cells=cells.map(c=>shifted(c,delta));}active.delete(id);}
const vol=concrete.reduce((sum,c)=>sum+volumeCg(cellFaces(c)).volumeM3,0),sourceVolume=volumeCg(s.cavity.facesMm).volumeM3;
assert.equal(parts.length,13);assert.ok(Math.abs(vol-sourceVolume)<1e-9,'Collision concrete differs from source mesh');
assert.equal(swept(box(0,1,0,1,0,1),box(2,3,0,1,0,1),[4,0,0]),true);assert.equal(swept(box(0,1,0,1,0,1),box(1,2,0,1,0,1),[-1,0,0]),false);assert.equal(swept(box(0,1,0,1,0,1),box(1,2,0,1,0,1),[1,0,0]),true);
return {status:initial.length||steps.some(x=>x.hits.length)?'FAIL':'PASS_NOMINAL_MODEL_ONLY',initialInterferences:initial,steps,sourceVolumeM3:sourceVolume,collisionVolumeM3:vol,algorithm:'continuous SAT of convex vertical prisms; 0.001mm penetration tolerance',limitations:['Removed parts are assumed transferred out of the working envelope before next step; transfer path NOT checked','No bolts, clamps, weld beads, seals, reinforcement, anchor hardware, temporary supports, workers or crane model','No shrinkage, adhesion, deformation, loads or fabrication tolerance; no structural or lifting approval','Arc geometry follows source 32-chord quarter-circle tessellation; production roll geometry and tolerances still require design'],engineeringApproved:false};}
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;');const tx=(x,y,s,n=24,c='#102e50')=>`<text x="${x}" y="${y}" font-size="${n}" fill="${c}">${esc(s)}</text>`;
const rect=(x,y,w,h,c='#fff')=>`<rect x="${x}" y="${y}" width="${w}" height="${h}" fill="${c}" stroke="#9aaec2"/>`;
const faces=(list,explode=false)=>list.flatMap(p=>p.cells.flatMap(c=>cellFaces(explode?shifted(c,p.exploded):c).map(face=>({face,color:p.color}))));
function render(ff,x,y,w,h,labels=[]){const proj=([x,y,z])=>[(x+y)/Math.sqrt(2),(-x+y)/Math.sqrt(6)-z*Math.sqrt(2/3)],depth=([x,y,z])=>(x-y-z)/Math.sqrt(3),ps=ff.flatMap(f=>f.face.map(proj)),mi=[0,1].map(i=>Math.min(...ps.map(p=>p[i]))),ma=[0,1].map(i=>Math.max(...ps.map(p=>p[i]))),scale=Math.min((w-50)/(ma[0]-mi[0]),(h-50)/(ma[1]-mi[1])),map=v=>proj(v).map((q,k)=>(q-mi[k])*scale+([w,h][k]-(ma[k]-mi[k])*scale)/2);let svg=`<image x="${x}" y="${y}" width="${w}" height="${h}" href="${raster(ff,w,h,map,depth)}"/>`;const placed=[];for(const label of labels){const p=map(label.point),q=[...p];while(placed.some(a=>Math.abs(a[0]-q[0])<62&&Math.abs(a[1]-q[1])<30))q[1]+=32;placed.push(q);svg+=`<path d="M${x+p[0]},${y+p[1]}L${x+q[0]},${y+q[1]}" stroke="#123d65"/>`;svg+=`<rect x="${x+q[0]-27}" y="${y+q[1]-15}" width="54" height="26" rx="4" fill="white" stroke="#123d65"/>`+tx(x+q[0]-21,y+q[1]+4,label.text,16);}return svg;}
const centre=p=>{const vs=p.cells.flatMap(c=>cellFaces(c).flat());return [0,1,2].map(i=>(Math.min(...vs.map(v=>v[i]))+Math.max(...vs.map(v=>v[i])))/2);};
const labels=(pp,explode=false)=>pp.map(p=>({text:p.id,point:add(centre(p),explode?p.exploded:[0,0,0])}));
const begin=(title,sub)=>`<svg xmlns="http://www.w3.org/2000/svg" width="2400" height="1700" viewBox="0 0 2400 1700"><rect width="2400" height="1700" fill="white"/><g font-family="Tahoma,Arial,sans-serif">${tx(35,55,title,37)}${tx(35,98,sub,24)}`;
const end=()=>rect(25,1590,2350,88,'#fff2df')+tx(45,1627,'P39 · PILOT PROPOSAL ONLY — 13 removable subassemblies; NOT RELEASED FOR FABRICATION / CASTING / LIFTING',23,'#8b492c')+tx(45,1660,'ขนาดเหล็กเป็นข้อเสนอเพื่อจำลอง geometry ยังไม่ตรวจแรงดันเท/โก่ง/จุดยึด/กำลังถอดแบบ · ไม่เปลี่ยนมิติคอนกรีตเดิม',22,'#8b492c')+'</g></svg>';
function overview(){let g=begin('TS-C-H15-LH-W01 | MOULD ASSEMBLY & DEMOULDING','ตัวอย่างใหม่ 1 Segment · แม่แบบเสนอ 13 ชุดย่อย · ทุกภาพฉายจากโมเดลเดียวกัน ไม่ใช้ generative image');g+=rect(25,125,1270,860)+rect(1310,125,1065,860);g+=tx(45,165,'A | EXPLODED — แยกชิ้นเพื่อแสดงส่วนประกอบ',27)+render(faces(parts,true),45,185,1230,680,labels(parts,true))+tx(45,930,'ระยะระเบิดภาพใช้จัดหน้าเท่านั้น; เส้นทางถอดที่ตรวจดูแผ่น 03',22);g+=tx(1330,165,'B | CONCRETE OUTPUT — รูปทรงต้นทาง',27)+render(s.cavity.facesMm.map(face=>({face,color:'#b9bdc1'})),1330,190,1025,570)+tx(1340,820,'ท่าหล่อ: ตั้งบนหน้าตัดปลาย ช่วงยาว 1,485 เป็นแนว Z',22)+tx(1340,858,'กรอบช่องหล่อ XYZ = 1490 × 2825 × 1485 mm',22)+tx(1340,896,'คอนกรีตหนา150 · โค้ง R400/R250 · หน้าต่าง900×1200',22)+tx(1340,934,`น้ำหนักคอนกรีต ${s.mass.concreteKg.toFixed(2)} kg (ไม่รวมเหล็ก/พุก)`,22);
g+=rect(25,1000,760,565)+rect(800,1000,760,565)+rect(1575,1000,800,565);g+=tx(45,1040,'C | ASSEMBLED — ช่องเทเปิดด้านบน',25)+render(faces(parts),45,1060,720,425)+tx(45,1520,'ไม่มีฝาปิดด้านบน; ต้องออกแบบจุดยึด/ค้ำก่อนเท',21);
g+=tx(820,1040,'D | WINDOW BOX-OUT — 4 ชิ้น',25)+render(faces(parts.filter(p=>p.id[0]==='W'),true),820,1060,720,400,labels(parts.filter(p=>p.id[0]==='W'),true))+tx(820,1510,'ปลดด้าน W03/W04 ก่อน แล้ว W01/W02',21)+tx(820,1545,'กลไกล็อก/ซีลและการรองรับบล็อกยังรอรายละเอียด',20);
g+=tx(1595,1040,'E | PART GROUPS / HOLD POINTS',25);['M00 ฐานและซี่รอง: 1 ชุด','M01–M03 ผิวนอก: ตรง / โค้ง / ตรง = 3','M04–M06 ผิวใน: ตรง / โค้ง / ตรง = 3','M07–M08 แบบปิดปลาย = 2','W01–W04 กรอบเว้นหน้าต่าง = 4','รวม 13 ชุดย่อย ไม่ใช่จำนวนแผ่นเหล็กตัด','skin6 / rib80×8 / bed12 เป็นขนาดทดลอง','ยังไม่รวมอุปกรณ์ล็อกและค้ำใน 13 ชุด','ฐานยังรองรับคอนกรีตหลังถอด; ไม่จำลองยก'].forEach((t,i)=>g+=tx(1595,1090+i*47,t,22));return g+end();}
function components(){let g=begin('02 | MOULD PART REGISTER — 13 SUBASSEMBLIES','TS-C-H15-LH-W01 · dimensions in mm · โครงสร้างและความหนาเครื่องมือเป็นข้อเสนอ ยังไม่ผ่านคำนวณ');for(let i=0;i<parts.length;i++){const p=parts[i],x=25+(i%5)*475,y=140+Math.floor(i/5)*470;g+=rect(x,y,460,450)+tx(x+15,y+35,`${p.id} | ${p.name}`,23)+render(faces([p]),x+12,y+55,436,290);const vs=p.cells.flatMap(c=>cellFaces(c).flat()),dims=[0,1,2].map(k=>Math.max(...vs.map(v=>v[k]))-Math.min(...vs.map(v=>v[k])));g+=tx(x+15,y+370,'Envelope XYZ '+dims.map(v=>v.toFixed(1)).join(' × '),17);const split=p.detail.split(';');split.forEach((t,j)=>g+=tx(x+15,y+401+j*24,t.trim(),18));}g+=tx(1465,1160,'แบบทุกชุดถอดออกจากกัน ไม่ใช่ผิวลอย',22)+tx(1465,1205,'ภาพแสดง skin และซี่โครงที่อยู่ในโมเดล',22)+tx(1465,1250,'จำนวนแผ่นย่อยและพิกัดอ่าน model.json',22)+tx(1465,1295,'จุดยึด/รอยเชื่อม/ระบบค้ำ: ยังไม่ออกแบบ',22);return g+end();}
function sequence(audit){let g=begin('03 | ASSEMBLY / STRIPPING STUDY — SAME MODEL','ตรวจการชนเชิง geometry ของ skin + ribs + bed + window parts กับคอนกรีตเดิม; ไม่รับรองแรงหรือวิธียก');const groups=[['1 | ฐาน + ผิวนอก',['M00','M01','M02','M03']],['2 | เพิ่มบล็อกช่องเปิด',['M00','M01','M02','M03','W01','W02','W03','W04']],['3 | ปิดผิวใน + ปลาย (ก่อนเท)',parts.map(p=>p.id)]];for(let i=0;i<3;i++){g+=rect(25+i*785,135,770,490)+tx(45+i*785,177,groups[i][0],25)+render(faces(parts.filter(p=>groups[i][1].includes(p.id))),45+i*785,200,730,385);}
g+=tx(40,665,'HOLD ก่อนเท: ตรวจแบบรับแรง / เหล็ก / จุดล็อก / ค้ำ / แผนเท และ HOLD ก่อนถอด: กำลังคอนกรีต + แผนรองรับ',24,'#a34725');
const st=[['4 | ถอดปลาย + ผิวนอก',['M00','M04','M05','M06','W01','W02','W03','W04']],['5 | ถอดผิวใน',['M00','W01','W02','W03','W04']],['6 | ถอดบล็อก / คอนกรีตยังบนฐาน',['M00']]];for(let i=0;i<3;i++){g+=rect(25+i*785,705,770,485)+tx(45+i*785,745,st[i][0],25)+render([...faces(parts.filter(p=>st[i][1].includes(p.id))),...s.cavity.facesMm.map(face=>({face,color:'#b9bdc1'}))],45+i*785,765,730,400);}
g+=tx(40,1240,'ลำดับและการเลื่อนออกที่ใช้ตรวจ (แกนท่าหล่อ ไม่ใช่แกนติดตั้งอาคาร)',26);
const lines=['M07: -Y300 → M08: +X300 → M01: -X500 → M03: +Y500 → M02: (-X500,+Y500)','M04: +X400 → M06: -Y400 → M05: (+X400,-Y400)','W03: +Y20 แล้ว -X250 → W04: -Y20 แล้ว -X250 → W01: +Z20 แล้ว -X250 → W02: -Z20 แล้ว -X250',`ผล SAT: ${audit.status} · การแทรก ${audit.initialInterferences.length+audit.steps.reduce((n,t)=>n+t.hits.length,0)} รายการในชิ้นที่จำลอง`,'แต่ละชิ้นต้องย้ายพ้นพื้นที่ทำงานก่อนขั้นถัดไป; เส้นทางขนย้าย เครื่องยก อุปกรณ์ล็อก/ค้ำ และเสถียรภาพยังไม่ตรวจ','ไม่มีการจำลองยกคอนกรีตออกจากฐาน ไม่ใช้ภาพนี้เป็นคำสั่งปฏิบัติงานโรงงาน'];lines.forEach((t,i)=>g+=tx(40,1290+i*46,t,21,i>=4?'#9b482c':'#183b56'));return g+end();}
function plan(){let g=begin('04 | CASTING DATUM / PROFILE / WINDOW','แยกมิติคอนกรีตต้นทางออกจากขนาดเครื่องมือที่เสนอ · มุมโค้งสัมผัสตาม mesh ต้นทาง R400/R250');const x=70,y=235,k=.36;const draw=(poly,color)=>`<polygon points="${poly.map(p=>[x+p[0]*k,y+(3025-p[1])*k].join(',')).join(' ')}" fill="${color}" stroke="#345777" stroke-width="1"/>`;g+=tx(50,170,'แปลน XY — ตัดผ่านท่าหล่อที่ Z=100 mm (ต่ำกว่าช่องเปิด)',23);for(const p of parts)for(const c of p.cells)if(c.z0<=100&&c.z1>=100)g+=draw(c.poly,p.color);for(const c of concrete)if(c.z0<=100&&c.z1>=100)g+=draw(c.poly,'#c7c9cb');g+=tx(50,1500,'ช่องคอนกรีต: X 0–1490 / Y 0–2825 / Z 0–1485',22);
g+=tx(950,170,'มิติและพิกัดสำหรับตรวจตัวอย่าง',28);const lines=['คอนกรีต: ผนัง–หลังคาหนาตั้งฉาก150; ช่วงหล่อ1485','แขนผนังตรง Y0–2425; แขนหลังคาตรง X400–1490','ศูนย์โค้ง XY=(400,2425); Rนอก400 / Rใน250','ช่อง W01 ในท่าหล่อ: X0–150 / Y900–2100 / Z292.5–1192.5','ดังนั้นช่องเปิดในท่าติดตั้ง: กว้าง900 × สูง1200','M01/M04 ผิวตรง: ยาว2425 × สูง1485; เว้นระยะผิว150','M03/M06 ผิวตรง: ยาว1090 × สูง1485; เว้นระยะผิว150','M02 radiusสัมผัส400 / M05 radiusสัมผัส250','แบบปิด M07/M08: หน้าคอนกรีต150 × สูง1485','ฐานเสนอ: X -200..1700 / Y -200..3025 / Zบน=0','ผิวฐานเสนอ12; ซี่รองฐานลึก150 หนา8','skinเครื่องมือเสนอ6; ซี่หลังเสนอ80ลึก × 8หนา','ซี่หลัง3ระดับ: Z150..158 / 738.5..746.5 / 1327..1335','ทุกขนาดเครื่องมือเป็น geometry trial ไม่ใช่ผลคำนวณ','ต้องเพิ่มรายละเอียด: ตัวล็อก จุดเชื่อม ซีล ค้ำ และตัวจับบล็อก','ต้องทบทวนแรงดันเท/การโก่ง/การทรุดฐานและความคลาดเคลื่อน','ตัวอย่างนี้ไม่รวมเหล็กเสริมหรือพุกยกที่ยังไม่ได้ออกแบบ'];lines.forEach((t,i)=>g+=tx(950,225+i*66,t,23,i>=13?'#9b482c':'#173954'));return g+end();}
export async function build(){fs.mkdirSync(out,{recursive:true});const audit=check();fs.writeFileSync(path.join(out,'collision-audit.json'),JSON.stringify(audit,null,2));if(audit.status==='FAIL')throw Error(JSON.stringify(audit));const req=createRequire(import.meta.url);let sharp;try{sharp=req('sharp');}catch{sharp=req('C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp');}
const model={revision:'P39-PILOT-R00',typicalId:s.typicalId,source,sourceSha256:hash(path.join(root,source)),castingTransform:s.castingTransform,units:'mm',partCount:parts.length,partCountBasis:'removable subassemblies incl bed; not cutting list or all hardware',parts:parts.map(p=>({...p,facesMm:p.cells.flatMap(cellFaces)})),concreteFacesMm:s.cavity.facesMm,concreteCollisionCells:concrete,removalSequence:moves,engineeringApproved:false,productionReleased:false,requiredUnmodelledItems:['locks','bolts','weld beads','seals','temporary support','handling tools','window hold-down','rebar','lifting inserts']};fs.writeFileSync(path.join(out,'model.json'),JSON.stringify(model,null,2));
for(const [name,svg]of [['01-OVERVIEW',overview()],['02-PARTS',components()],['03-SEQUENCE',sequence(audit)],['04-DIMENSIONS',plan()]]){fs.writeFileSync(path.join(out,name+'.svg'),svg);await sharp(Buffer.from(svg)).png().toFile(path.join(out,name+'.png'));}
fs.writeFileSync(path.join(out,'README.md'),['# ตัวอย่างแม่แบบ P39 — TS-C-H15-LH-W01 เท่านั้น','','เสนอแม่แบบเหล็กถอดแยก13ชุดย่อย มีผิวแบบและซี่โครงในโมเดล ไม่ใช่เพียงผิวช่องหล่อ ไม่แก้แบบหรือเว็บเดิม ไม่ขยาย44รายการก่อนผู้ใช้ตรวจ','','[ภาพรวม](01-OVERVIEW.png) · [13ชุดย่อย](02-PARTS.png) · [ประกอบ/ถอด](03-SEQUENCE.png) · [มิติ](04-DIMENSIONS.png)','','## บัญชีชิ้นแม่แบบ','| Tag | ชุดย่อย | มิติ/ข้อเสนอ |','|---|---|---|',...parts.map(p=>`|${p.id}|${p.name}|${p.detail}|`),'','## ระดับการตรวจ','คอนกรีตแสดงด้วยfacesต้นทางโดยไม่เปลี่ยน; solidสำหรับตรวจชนมีปริมาตรเท่ากับmeshต้นทาง ตรวจโมเดลประกอบและเส้นทางเลื่อนด้วยcontinuous SAT ไม่ใช่ตรวจเฉพาะภาพต้น/ปลาย ดูcollision-audit.json','ผลผ่านเฉพาะskin/ribs/base/windowในโมเดล nominal การย้ายเครื่องมือไปจอดนอกพื้นที่ทำงานไม่นับว่าตรวจแล้ว ไม่รวมlocks/bolts/weld/seals/rebar/anchors/crane/temporary supports และไม่ตรวจการยกคอนกรีต','steel skin6, rib80×8, baseplate12, base ribs150×8 เป็นข้อเสนอสำหรับ geometry ไม่ใช่ผลออกแบบความแข็งแรง ห้ามสั่งผลิต/เท/ถอด/ยกจากชุดนี้','ก่อนถอดต้องออกแบบการรองรับชิ้นคอนกรีตและแม่แบบ รวมระบบจับบล็อกหน้าต่างมิให้หล่น ไม่ใช้ชิ้นที่ลอยในภาพexplodedเป็นคำสั่งหน้างาน','','## ลำดับประกอบเสนอ','ฐาน → ผิวนอก → กรอบหน้าต่างพร้อมตัวยึดที่ต้องออกแบบ → เหล็ก/พุกตามแบบที่จะอนุมัติ → ผิวใน/แบบปิดปลาย → จุดล็อก/ค้ำ/ซีลและตรวจมิติ → HOLD อนุมัติแผนเท/แบบ → เทและบ่ม','ลำดับถอดผิวในภาพ03เป็นการศึกษาทางเดินคงรูป; การรองรับกำลังและลำดับปฏิบัติจริงต้องตรวจอีกครั้ง','','## อ้างอิงขอบเขตการออกแบบ','[ACI Guide to Formwork — design, construction, materials and form removal](https://www.concrete.org/portals/0/files/pdf/previews/347r-14_preview.pdf) ใช้ยืนยันหัวข้อที่ต้องตรวจ ไม่ได้อ้างว่าผ่านการออกแบบตามACIหรือวสท.','','## สถานะ','ตัวอย่างส่งตรวจ 1 ชิ้น ไม่ใช่จบขั้น5; ขั้น5/8คง50% รอความคิดเห็นผู้ใช้ก่อนแก้ของเดิมหรือทำรายการที่เหลือ',''].join('\n'));console.log(JSON.stringify({out,parts:parts.length,collision:audit.status,steps:audit.steps.length}));}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url))await build();
