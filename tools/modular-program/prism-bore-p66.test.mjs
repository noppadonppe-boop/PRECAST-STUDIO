import {test} from 'node:test';import assert from 'node:assert/strict';import {bores} from './prism-bore-p66.mjs';import {rect,properties,collide,disk} from './prism-tools-p54.mjs';
test('blind bore removes exactly the nominal polygon-cylinder volume, not underlying bed',()=>{const a=[rect(-50,50,-50,50,-30,0)],h={x:0,y:0,r:10,z0:-20,z1:0},b=bores(a,[h]),expected=properties([disk(0,0,10,-20,0)]).volumeMm3;assert.ok(Math.abs(properties(a).volumeMm3-properties(b).volumeMm3-expected)<1e-5);assert.equal(collide(b,[disk(0,0,10,-20,0)]),false);assert.equal(collide(b,[disk(0,0,10,-30,-20)]),true);});
test('a bore crossing two sloping cells removes material once and leaves no overlaps',()=>{const a=[{poly:[[0,0],[100,20],[100,100]],z0:0,z1:30},{poly:[[0,0],[100,100],[0,80]],z0:0,z1:30}],h={x:50,y:50,r:11,z0:0,z1:30},b=bores(a,[h]),expected=properties([disk(50,50,11,0,30)]).volumeMm3;assert.ok(Math.abs(properties(a).volumeMm3-properties(b).volumeMm3-expected)<1e-5);for(let i=0;i<b.length;i++)for(let j=0;j<i;j++)assert.equal(collide([b[i]],[b[j]]),false);});
test('disjoint bore leaves material volume unchanged',()=>{const a=[rect(0,100,0,100,0,30)],b=bores(a,[{x:300,y:300,r:10,z0:0,z1:30}]);assert.deepEqual(a,b);});
