import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {chromium} from '@playwright/test';import {pathToFileURL} from 'node:url';import assert from 'node:assert/strict';
const dir='output/window-shell-p82',browser=await chromium.launch({headless:true,channel:'chrome'});try{const p=await browser.newPage({viewport:{width:1450,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(pathToFileURL(path.resolve(dir,'index.html')).href);assert.equal(await p.locator('details').count(),12);for(const img of await p.locator('img').all())await img.evaluate(async e=>{await e.decode();if(e.naturalWidth!==1450)throw Error('Unexpected image');});const links=await p.locator('a[download]').evaluateAll(as=>as.map(a=>a.getAttribute('href')));assert.equal(links.length,72);for(const href of links)assert.ok(fs.existsSync(path.join(dir,href)));await p.locator('details').first().evaluate(e=>e.open=true);await p.screenshot({path:dir+'/report-preview.png'});assert.deepEqual(errors,[]);fs.writeFileSync(dir+'/browser-audit.json',JSON.stringify({imagesDecoded:12,downloadFilesPresent:72,pageErrors:errors},null,2));}finally{await browser.close();}
const out='output/stage5-update-p82';fs.mkdirSync(out,{recursive:true});fs.cpSync(dir,out+'/window-shell-p82',{recursive:true});fs.mkdirSync(out+'/source-scripts',{recursive:true});for(const s of ['window-shell-input-p82.mjs','window_shell_p82.py','run_window_shell_p82.py','window-shell-p82.test.mjs','window-shell-report-p82.mjs'])fs.copyFileSync('tools/modular-program/'+s,out+'/source-scripts/'+s);fs.copyFileSync('knowledge/modular-program-r02/STAGE5_CONTINUATION_P82.md',out+'/KNOWLEDGE.md');fs.writeFileSync(out+'/test-results.txt',execFileSync(process.execPath,['--test','tools/modular-program/window-shell-p82.test.mjs'],{encoding:'utf8'}));fs.writeFileSync(out+'/README.md','# P82 window wall shell/rib study\n\nOpen window-shell-p82/index.html. 12 wall panels from6 W01 setups,36 mesh runs,12 model-derived displacement PNG/SVG boards. Ideal rigid waler interface case, NOT whole-mould behaviour or code capacity verification. M03 rear skin remains solid with a pressure-free window zone. Local stress convergence is NOT verified. No geometry changes, lifting or production approval. Scripts require the existing project sources and local OpenSees runtime; private runtime binaries are not included.\n');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);const files=walk(out).filter(p=>path.basename(p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(out+'/manifest.json',JSON.stringify({revision:'P82',stageComplete:false,engineeringApproved:false,productionReleased:false,files},null,2));console.log({files:files.length,bytes:files.reduce((s,f)=>s+f.bytes,0)});
