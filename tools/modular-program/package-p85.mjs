import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {chromium} from '@playwright/test';import {pathToFileURL} from 'node:url';import assert from 'node:assert/strict';
const dir='output/abd-base-pattern-p85',browser=await chromium.launch({headless:true,channel:'chrome'});
try{const p=await browser.newPage({viewport:{width:1500,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(pathToFileURL(path.resolve(dir,'index.html')).href);assert.equal(await p.locator('details').count(),12);for(const img of await p.locator('img').all())await img.evaluate(async e=>{await e.decode();if(e.naturalWidth!==1650||e.naturalHeight!==1320)throw Error('Unexpected board');});const links=await p.locator('a[download]').evaluateAll(as=>as.map(a=>a.getAttribute('href')));assert.equal(links.length,51);for(const href of links)assert.ok(fs.existsSync(path.join(dir,href)));await p.locator('details').first().evaluate(e=>e.open=true);await p.screenshot({path:dir+'/report-preview.png'});await p.setViewportSize({width:390,height:844});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);fs.writeFileSync(dir+'/browser-audit.json',JSON.stringify({imagesDecoded:12,downloadsPresent:51,mobileOverflow:false,pageErrors:errors},null,2));}finally{await browser.close();}
const out='output/stage5-update-p85';fs.mkdirSync(out,{recursive:true});fs.cpSync(dir,out+'/abd-base-pattern-p85',{recursive:true});fs.mkdirSync(out+'/source-scripts',{recursive:true});for(const s of ['abd-base-pattern-p85.mjs','abd-base-pattern-p85.test.mjs','base-demand-p85.mjs','base-pattern-boards-p85.mjs','package-p85.mjs'])fs.copyFileSync('tools/modular-program/'+s,out+'/source-scripts/'+s);fs.copyFileSync('knowledge/modular-program-r02/STAGE5_CONTINUATION_P85.md',out+'/KNOWLEDGE.md');fs.writeFileSync(out+'/test-results.txt',execFileSync(process.execPath,['--test','tools/modular-program/abd-base-pattern-p85.test.mjs'],{encoding:'utf8'}));fs.writeFileSync(out+'/README.md','# P85 — Revised base-lock geometry\n\nOpen abd-base-pattern-p85/index.html.12 A/B/D LH/RH S00/W01 base models,12 source-CAD boards and40-axis bore tables per model. Eight near-wall bolts and washers relocated per setup; changed parts/obstacles checked continuously and76 socket bodies checked against combined geometry. Use P85 base + P71 web + P74 seam + P80 edge stock once, with P85 sequence. Prior overlays remain in the project; this is a revision supplement, not a standalone fabrication package.\n\nW01 bolt demands use validated P84 option geometry and recomputed shear; no S00 demand extrapolation. No plate/thread/weld/lifting capacity or production release. Stage5 remains incomplete. Scripts need the existing project dependencies and inputs.\n');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]),files=walk(out).filter(p=>path.basename(p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(out+'/manifest.json',JSON.stringify({revision:'P85',stageComplete:false,engineeringApproved:false,productionReleased:false,files},null,2));console.log({files:files.length,bytes:files.reduce((a,f)=>a+f.bytes,0)});
