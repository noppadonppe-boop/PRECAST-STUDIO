import fs from 'node:fs';import {keys} from './abd-edge-ribs-p78.mjs';import {combine} from './abd-mass-p70.mjs';
const dir='output/abd-edge-ribs-p80',all=keys.map(key=>{const m=JSON.parse(fs.readFileSync(`${dir}/${key}.json`)),previous=JSON.parse(fs.readFileSync(`output/abd-load-ledger-p75/${key}.json`));return {...m,totalSteelMassAndCG:combine([previous.emptyMould,...m.stock.map(p=>p.properties)])};}),n=v=>v.toFixed(3);
fs.writeFileSync(`${dir}/register.json`,JSON.stringify({revision:'P80',stage:5,stageComplete:false,records:all.map(m=>({key:m.key,id:m.id,addedMassKg:m.addedMassKg,totalSteelMassAndCG:m.totalSteelMassAndCG,inputs:m.inputSnapshots,status:m.status}))},null,2));
fs.writeFileSync(`${dir}/index.html`,`<!doctype html><html lang="th"><meta charset="utf-8"><title>P80 — ซี่ขอบและทางถอด</title><style>body{font:16px Tahoma,Arial;color:#102c50;max-width:1400px;margin:28px auto;padding:16px}.note{background:#fff0d7;padding:18px}table{border-collapse:collapse;width:100%;margin:20px 0}th,td{border:1px solid #bdc7d0;padding:8px;text-align:right}td:first-child,th:first-child{text-align:left}details{margin:20px 0;overflow:auto}</style><h1>ขั้น 5/8 · P80 — ซี่ขอบและทางถอนฮาร์ดแวร์ฉบับแก้</h1><p class="note">12/12 รุ่นผ่านการตรวจ geometry ส่วนเพิ่ม ไม่ใช่ขั้น5ครบ100% หรือรับรองกำลัง<br>ใช้ P66 + P71 + P74 + P80 เท่านั้น ไม่บวก P78/P79 ซ้ำ<br>คานรับซี่ผนังที่ต่อยาวให้ผลิตเป็นชิ้นเต็มความยาวใหม่ การแบ่งเป็น EXT ในข้อมูลเป็นการแสดงส่วนเพิ่ม ไม่ใช่รอยต่อเชื่อมที่อนุมัติ</p><table><tr><th>รุ่น</th><th>ซี่+ส่วนต่อคาน</th><th>เหล็กเพิ่ม kg</th><th>เหล็กรวมโมเดล kg</th><th>ช่วงถอดที่ตรวจ</th></tr>`+all.map(m=>`<tr><td><a href="${m.key}.json" download>${m.key}</a></td><td>${m.stock.length}</td><td>${n(m.addedMassKg)}</td><td>${n(m.totalSteelMassAndCG.massKg)}</td><td>${m.audit.supplementaryMoves.length}</td></tr>`).join('')+'</table><p>ปรับทางถอน SJ-M03-M04: คลาย/ถอนตามแกนZ±100 → เลื่อน-Y100 → เคลื่อนXเข้าด้านใน500มม. ล็อกล่าง L1 ของ A/B เพิ่มยกZ600ก่อนเคลื่อนX เพื่อหลบค้ำหลังคา; D ไม่ต้องยกช่วงนี้ ทิศXของ RH กลับเครื่องหมายจาก LH</p><p>แรง/โก่งด้านล่างเป็นคานแถบสมมติที่แกนซี่ยึดการเลื่อน ไม่ใช่ผลโครงแม่แบบทั้งชุด โซนหน้าต่างไม่อยู่ในการตรวจแถบนี้ ผิวโค้งBแทนแถบราบ ไม่รับรองขอบเขตบน</p>'+all.map(m=>`<details><summary>${m.key} — ผลแถบตามตำแหน่งซี่ฉบับนี้</summary><table><tr><th>ผิว</th><th>ผิวเหล็ก mm</th><th>elastic stress MPa</th><th>elastic deflection mm</th></tr>`+m.stripStudy.flatMap(p=>p.cases.map(c=>`<tr><td>${p.owner}</td><td>${c.thicknessMm}</td><td>${n(c.maxElasticStressMPa)}</td><td>${n(c.maxElasticDeflectionMm)}</td></tr>`)).join('')+'</table></details>').join('')+'<p>ยังไม่เปลี่ยนผิว6มม.เป็นความหนาใหม่ ต้องตรวจการโก่ง/แรงร่วมรอยต่อ โครงหลัก งานเชื่อมและฐานก่อนเลือก ระบบยกและแบบผลิตยังไม่อนุมัติ</p></html>');
