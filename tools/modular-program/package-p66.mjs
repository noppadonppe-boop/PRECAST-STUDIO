import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {root} from './cap-geometry-p55.mjs';
const out=path.join(root,'output/stage5-update-p66');fs.mkdirSync(out,{recursive:true});fs.cpSync(path.join(root,'output/abd-base-lock-p66'),path.join(out,'abd-base-lock-p66'),{recursive:true});fs.mkdirSync(path.join(out,'source-scripts'),{recursive:true});
for(const n of ['abd-base-lock-p66.mjs','abd-base-boards-p66.mjs','abd-base-lock-p66.test.mjs','prism-bore-p66.mjs','prism-bore-p66.test.mjs'])fs.copyFileSync(path.join(root,'tools/modular-program',n),path.join(out,'source-scripts',n));
fs.copyFileSync(path.join(root,'knowledge/modular-program-r02/STAGE5_CONTINUATION_P66.md'),path.join(out,'KNOWLEDGE.md'));
fs.writeFileSync(path.join(out,'test-results.txt'),execFileSync(process.execPath,['--test','tools/modular-program/abd-base-lock-p66.test.mjs'],{cwd:root,encoding:'utf8'}));
fs.writeFileSync(path.join(out,'README.md'),'# P66 — base feet and locks\n\nOpen abd-base-lock-p66/index.html. Twelve coordinated model-derived boards, JSON geometry and lock-axis CSVs. Braces, seam locks and structural/handling/fabrication checks remain incomplete. Nominal thread envelopes are not manufacturing tap sizes. No fabrication/casting/lifting release. Scripts require the existing project. Website remains P61 pending integrated hardware.\n');
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]),files=walk(out).filter(p=>path.relative(out,p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(path.join(out,'manifest.json'),JSON.stringify({revision:'P66',stage:5,stageComplete:false,files,engineeringApproved:false,productionReleased:false},null,2));console.log({files:files.length});
