import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {root} from './cap-geometry-p55.mjs';
const out=path.join(root,'output/stage5-update-p71');fs.mkdirSync(out,{recursive:true});fs.cpSync(path.join(root,'output/abd-roof-web-p71'),path.join(out,'abd-roof-web-p71'),{recursive:true});fs.mkdirSync(path.join(out,'source-scripts'),{recursive:true});
for(const n of ['abd-roof-web-p71.mjs','abd-roof-web-boards-p71.mjs','abd-roof-web-p71.test.mjs'])fs.copyFileSync(path.join(root,'tools/modular-program',n),path.join(out,'source-scripts',n));
fs.copyFileSync(path.join(root,'knowledge/modular-program-r02/STAGE5_CONTINUATION_P71.md'),path.join(out,'KNOWLEDGE.md'));
fs.writeFileSync(path.join(out,'test-results.txt'),execFileSync(process.execPath,['--test','tools/modular-program/abd-roof-web-p71.test.mjs'],{cwd:root,encoding:'utf8'}));
fs.writeFileSync(path.join(out,'README.md'),'# P71 — roof base webs\n\nOpen abd-roof-web-p71/index.html. Twelve coordinated model-derived boards, JSON geometry and gusset-stock CSVs. Braces, seam locks and structural/handling/fabrication checks remain incomplete. Curved roof webs are profiled solids, not nominal constant-thickness flat plates; fabrication details remain unresolved. No fabrication/casting/lifting release. Scripts require the existing project. Website remains P68 pending integrated hardware.\n');
const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]),files=walk(out).filter(p=>path.relative(out,p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(path.join(out,'manifest.json'),JSON.stringify({revision:'P71',stage:5,stageComplete:false,files,engineeringApproved:false,productionReleased:false},null,2));console.log({files:files.length});
