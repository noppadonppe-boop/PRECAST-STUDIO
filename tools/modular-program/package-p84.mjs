import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {execFileSync} from 'node:child_process';import {chromium} from '@playwright/test';import {pathToFileURL} from 'node:url';import assert from 'node:assert/strict';
const dir='output/foot-demand-p84',browser=await chromium.launch({headless:true,channel:'chrome'});
try{const page=await browser.newPage({viewport:{width:1500,height:1000}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(pathToFileURL(path.resolve(dir,'index.html')).href);assert.equal(await page.locator('details').count(),24);for(const img of await page.locator('img').all())await img.evaluate(async e=>{await e.decode();if(e.naturalWidth!==1600||e.naturalHeight!==1100)throw Error('Unexpected image dimensions');});const links=await page.locator('a[download]').evaluateAll(as=>as.map(a=>a.getAttribute('href')));assert.equal(links.length,99);for(const href of links)assert.ok(fs.existsSync(path.join(dir,href)));await page.locator('details').first().evaluate(e=>e.open=true);await page.screenshot({path:dir+'/report-preview.png'});await page.setViewportSize({width:390,height:844});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);fs.writeFileSync(dir+'/browser-audit.json',JSON.stringify({imagesDecoded:24,downloadsPresent:99,mobileOverflow:false,pageErrors:errors},null,2));}finally{await browser.close();}
const out='output/stage5-update-p84';fs.mkdirSync(out,{recursive:true});fs.cpSync(dir,out+'/foot-demand-p84',{recursive:true});fs.mkdirSync(out+'/source-scripts',{recursive:true});for(const s of ['foot-contact-p84.mjs','foot-contact-p84.test.mjs','foot-demand-p84.mjs','foot-demand-p84.test.mjs','foot-pattern-option-p84.mjs','foot-report-p84.mjs','package-p84.mjs'])fs.copyFileSync('tools/modular-program/'+s,out+'/source-scripts/'+s);fs.copyFileSync('knowledge/modular-program-r02/STAGE5_CONTINUATION_P84.md',out+'/KNOWLEDGE.md');fs.writeFileSync(out+'/test-results.txt',execFileSync(process.execPath,['--test','tools/modular-program/foot-contact-p84.test.mjs','tools/modular-program/foot-demand-p84.test.mjs'],{encoding:'utf8'}));fs.writeFileSync(out+'/README.md','# P84 — Base-foot and bolt demand\n\nOpen foot-demand-p84/index.html. 24 current foot groups,96 bolts,216 current-pattern stiffness scenarios plus216 candidate scenarios. Rigid plate and stationary compression-only bed with tension-only bolts. NOT plate bending/prying, thread strength, floor strength or production approval. Candidate hole pair relocation is analysed for tension but not yet collision/handling/shear verified; current P66 geometry remains unchanged. Stage5 is NOT complete. Scripts require existing project dependencies/source models.\n');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]),files=walk(out).filter(p=>path.basename(p)!=='manifest.json').map(p=>({path:path.relative(out,p).replaceAll('\\','/'),bytes:fs.statSync(p).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}));fs.writeFileSync(out+'/manifest.json',JSON.stringify({revision:'P84',stageComplete:false,engineeringApproved:false,productionReleased:false,files},null,2));console.log({files:files.length,bytes:files.reduce((a,f)=>a+f.bytes,0)});
