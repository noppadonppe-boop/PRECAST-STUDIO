# Calculation worker boundary

Reserved for a future verified Cloud Run solver adapter. M0 contains no solver implementation. Selection of a solver, design code, benchmark suite or production runtime requires Product Owner approval.

